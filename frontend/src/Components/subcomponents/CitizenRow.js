import React from 'react';

export default CitizenRow(props) {
    return(
        
        <tbody>
            {props.data.map(element => {
                
            })}
        </tbody>
        
    )
}